# --- A2A demo (Stellar testnet) ---
A2A_NURSE_PUBLIC=""
A2A_NURSE_SECRET=""        # signs withdraw() because the contract does to.require_auth()
A2A_PRIVACY_POOL_ID=""     # set by scripts/a2a-setup.sh per deploy (single-use today)
A2A_NETWORK="testnet"

# --- Optional: Claude API key for the nurse reply ---
ANTHROPIC_API_KEY=""
