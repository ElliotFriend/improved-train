# --- A2A demo (Stellar testnet) ---
# Patient + nurse are server-managed testnet accounts (the user's browser does
# not hold their keys). Fund both with friendbot before first run.
A2A_PATIENT_SECRET=""
A2A_PATIENT_PUBLIC=""
A2A_NURSE_SECRET=""
A2A_NURSE_PUBLIC=""
A2A_AMOUNT_XLM="1"

# --- Optional: Claude API key for the nurse to generate replies ---
# If unset, the nurse endpoint returns a placeholder confirming the payment.
ANTHROPIC_API_KEY=""
